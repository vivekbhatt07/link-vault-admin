const SignInFooter = () => {
  return (
    <p className="mx-auto text-center text-xs text-muted-foreground">
      Admin access is granted directly in the database. There is no sign-up
      here.
    </p>
  );
};

export default SignInFooter;
